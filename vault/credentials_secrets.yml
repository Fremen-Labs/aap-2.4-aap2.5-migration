credential_secret_map:
  # "OrgName:/ Credential Name": { username: "...", password: "{{ vault_linux_password }}" }
  # Add mappings as needed; merge into credential inputs before import if we choose that flow