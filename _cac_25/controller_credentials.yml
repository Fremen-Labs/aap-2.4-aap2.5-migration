controller_credentials:
  - name: My Credential
    description: Optional
    organization: Example Org
    credential_type: Machine
    inputs: {}          # secrets injected at runtime
    state: present